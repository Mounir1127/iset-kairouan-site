<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ISET Kairouan - Connexion</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-blue: #0055a4;
            --secondary-blue: #0077c8;
            --light-blue: #e6f2ff;
            --accent-blue: #003366;
            --white: #ffffff;
            --dark-gray: #333333;
            --medium-gray: #666666;
            --light-gray: #f5f7fa;
            --success-green: #2e7d32;
            --error-red: #d32f2f;
            --warning-orange: #f57c00;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', 'Inter', 'Roboto', sans-serif;
        }

        body {
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--secondary-blue) 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        .login-container {
            width: 100%;
            max-width: 1200px;
            min-height: 600px;
            display: flex;
            background-color: var(--white);
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
        }

        /* Left Side - Branding */
        .login-left {
            flex: 1;
            background: linear-gradient(135deg, var(--primary-blue) 0%, var(--accent-blue) 100%);
            color: var(--white);
            padding: 4rem 3rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .login-left::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" preserveAspectRatio="none"><path d="M0,0 L100,0 L100,100 Z" fill="rgba(255,255,255,0.05)"/></svg>');
            background-size: cover;
        }

        .brand-logo {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            margin-bottom: 3rem;
        }

        .logo-circle {
            width: 80px;
            height: 80px;
            background-color: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            font-weight: bold;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        }

        .brand-text h1 {
            font-size: 2.2rem;
            font-weight: 800;
            margin-bottom: 0.5rem;
            letter-spacing: -0.5px;
        }

        .brand-text p {
            font-size: 1rem;
            opacity: 0.9;
        }

        .features-list {
            margin: 3rem 0;
        }

        .feature-item {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .feature-icon {
            width: 40px;
            height: 40px;
            background-color: rgba(255, 255, 255, 0.15);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
        }

        .feature-text h3 {
            font-size: 1.1rem;
            margin-bottom: 0.2rem;
        }

        .feature-text p {
            font-size: 0.9rem;
            opacity: 0.8;
        }

        .testimonial {
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            padding: 1.5rem;
            margin-top: auto;
            border-left: 4px solid var(--white);
        }

        .testimonial p {
            font-style: italic;
            margin-bottom: 0.8rem;
        }

        .testimonial-author {
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }

        .author-avatar {
            width: 36px;
            height: 36px;
            background-color: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .author-info h4 {
            font-size: 0.95rem;
            font-weight: 600;
        }

        .author-info span {
            font-size: 0.85rem;
            opacity: 0.8;
        }

        /* Right Side - Login Form */
        .login-right {
            flex: 1;
            padding: 4rem 3rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .login-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .login-header h2 {
            color: var(--accent-blue);
            font-size: 2.2rem;
            font-weight: 800;
            margin-bottom: 0.8rem;
        }

        .login-header p {
            color: var(--medium-gray);
            font-size: 1.1rem;
        }

        .login-form {
            width: 100%;
            max-width: 400px;
            margin: 0 auto;
        }

        .form-group {
            margin-bottom: 1.8rem;
        }

        .form-label {
            display: block;
            margin-bottom: 0.6rem;
            color: var(--accent-blue);
            font-weight: 600;
            font-size: 0.95rem;
        }

        .input-with-icon {
            position: relative;
        }

        .form-input {
            width: 100%;
            padding: 1rem 1rem 1rem 3.5rem;
            border: 2px solid rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s;
            background-color: var(--light-gray);
        }

        .form-input:focus {
            outline: none;
            border-color: var(--primary-blue);
            box-shadow: 0 0 0 3px rgba(0, 85, 164, 0.1);
            background-color: var(--white);
        }

        .input-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--medium-gray);
            font-size: 1.2rem;
        }

        .password-toggle {
            position: absolute;
            right: 1rem;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: var(--medium-gray);
            cursor: pointer;
            font-size: 1.2rem;
        }

        .form-options {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            font-size: 0.9rem;
        }

        .remember-me {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--medium-gray);
        }

        .remember-me input[type="checkbox"] {
            width: 18px;
            height: 18px;
            border-radius: 4px;
            border: 2px solid var(--primary-blue);
            cursor: pointer;
        }

        .forgot-password {
            color: var(--primary-blue);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s;
        }

        .forgot-password:hover {
            color: var(--accent-blue);
            text-decoration: underline;
        }

        .submit-btn {
            width: 100%;
            padding: 1.2rem;
            background: linear-gradient(to right, var(--primary-blue), var(--secondary-blue));
            color: var(--white);
            border: none;
            border-radius: 10px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.8rem;
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0, 85, 164, 0.3);
        }

        .submit-btn:active {
            transform: translateY(-1px);
        }

        .login-divider {
            text-align: center;
            position: relative;
            margin: 2.5rem 0;
            color: var(--medium-gray);
        }

        .login-divider::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 1px;
            background-color: rgba(0, 0, 0, 0.1);
        }

        .login-divider span {
            background-color: var(--white);
            padding: 0 1.5rem;
            position: relative;
            font-size: 0.9rem;
        }

        .social-login {
            display: flex;
            gap: 1rem;
            margin-bottom: 2.5rem;
        }

        .social-btn {
            flex: 1;
            padding: 1rem;
            border: 2px solid rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            background: var(--white);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.8rem;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: 600;
            font-size: 0.95rem;
        }

        .social-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            border-color: var(--primary-blue);
        }

        .social-btn.google {
            color: #db4437;
        }

        .social-btn.microsoft {
            color: #00a4ef;
        }

        .register-link {
            text-align: center;
            color: var(--medium-gray);
            font-size: 0.95rem;
        }

        .register-link a {
            color: var(--primary-blue);
            text-decoration: none;
            font-weight: 600;
            margin-left: 0.5rem;
        }

        .register-link a:hover {
            text-decoration: underline;
        }

        /* Alert Messages */
        .alert {
            padding: 1rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            animation: slideIn 0.3s ease;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .alert-success {
            background-color: rgba(46, 125, 50, 0.1);
            color: var(--success-green);
            border: 1px solid rgba(46, 125, 50, 0.2);
        }

        .alert-error {
            background-color: rgba(211, 47, 47, 0.1);
            color: var(--error-red);
            border: 1px solid rgba(211, 47, 47, 0.2);
        }

        .alert-warning {
            background-color: rgba(245, 124, 0, 0.1);
            color: var(--warning-orange);
            border: 1px solid rgba(245, 124, 0, 0.2);
        }

        .alert-icon {
            font-size: 1.2rem;
        }

        /* Responsive */
        @media (max-width: 992px) {
            .login-container {
                flex-direction: column;
                max-width: 500px;
            }
            
            .login-left {
                padding: 3rem 2rem;
            }
            
            .login-right {
                padding: 3rem 2rem;
            }
        }

        @media (max-width: 480px) {
            body {
                padding: 1rem;
            }
            
            .login-container {
                border-radius: 15px;
            }
            
            .login-left, .login-right {
                padding: 2rem 1.5rem;
            }
            
            .social-login {
                flex-direction: column;
            }
            
            .brand-text h1 {
                font-size: 1.8rem;
            }
            
            .login-header h2 {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <!-- Left Side - Branding -->
        <div class="login-left">
            <div class="brand-logo">
                <div class="logo-circle">IK</div>
                <div class="brand-text">
                    <h1>ISET KAIR OUAN</h1>
                    <p>Institut Supérieur des Études Technologiques</p>
                </div>
            </div>

            <div class="features-list">
                <div class="feature-item">
                    <div class="feature-icon">
                        <i class="fas fa-graduation-cap"></i>
                    </div>
                    <div class="feature-text">
                        <h3>Formations Certifiées</h3>
                        <p>Diplômes reconnus par l'État</p>
                    </div>
                </div>
                
                <div class="feature-item">
                    <div class="feature-icon">
                        <i class="fas fa-laptop-code"></i>
                    </div>
                    <div class="feature-text">
                        <h3>Plateforme Numérique</h3>
                        <p>Accès 24/7 aux ressources pédagogiques</p>
                    </div>
                </div>
                
                <div class="feature-item">
                    <div class="feature-icon">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <div class="feature-text">
                        <h3>Suivi Personnalisé</h3>
                        <p>Accompagnement individualisé</p>
                    </div>
                </div>
            </div>

            <div class="testimonial">
                <p>"La plateforme de l'ISET Kairouan a révolutionné mon apprentissage. Interface intuitive et ressources complètes."</p>
                <div class="testimonial-author">
                    <div class="author-avatar">
                        <i class="fas fa-user"></i>
                    </div>
                    <div class="author-info">
                        <h4>Ahmed Ben Salah</h4>
                        <span>Étudiant en Génie Informatique</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Right Side - Login Form -->
        <div class="login-right">
            <!-- Alert Message (visible only on error/success) -->
            <div class="alert alert-warning" id="infoAlert" style="display: none;">
                <i class="fas fa-info-circle alert-icon"></i>
                <div>Pour accéder à l'espace étudiant officiel, veuillez utiliser le lien <a href="https://isetkairouan.edx.tn/login.faces" style="color: inherit; font-weight: bold;">https://isetkairouan.edx.tn</a></div>
            </div>

            <div class="login-header">
                <h2>Connexion</h2>
                <p>Accédez à votre espace personnel</p>
            </div>

            <form class="login-form" id="loginForm" onsubmit="handleLogin(event)">
                <div class="form-group">
                    <label class="form-label" for="username">Identifiant</label>
                    <div class="input-with-icon">
                        <i class="fas fa-user input-icon"></i>
                        <input type="text" class="form-input" id="username" placeholder="Matricule ou Email académique" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label class="form-label" for="password">Mot de passe</label>
                    <div class="input-with-icon">
                        <i class="fas fa-lock input-icon"></i>
                        <input type="password" class="form-input" id="password" placeholder="Votre mot de passe" required>
                        <button type="button" class="password-toggle" onclick="togglePassword('password')">
                            <i class="fas fa-eye"></i>
                        </button>
                    </div>
                </div>
                
                <div class="form-options">
                    <label class="remember-me">
                        <input type="checkbox" id="rememberMe">
                        <span>Se souvenir de moi</span>
                    </label>
                    <a href="#" class="forgot-password" onclick="showForgotPassword()">Mot de passe oublié ?</a>
                </div>
                
                <button type="submit" class="submit-btn">
                    <i class="fas fa-sign-in-alt"></i>
                    Se connecter
                </button>

                <div class="login-divider">
                    <span>Ou connectez-vous avec</span>
                </div>
                
                <div class="social-login">
                    <button type="button" class="social-btn google" onclick="socialLogin('google')">
                        <i class="fab fa-google"></i>
                        Google
                    </button>
                    <button type="button" class="social-btn microsoft" onclick="socialLogin('microsoft')">
                        <i class="fab fa-microsoft"></i>
                        Microsoft
                    </button>
                </div>
                
                <div class="register-link">
                    <span>Pas encore de compte ?</span>
                    <a href="#" onclick="showRegister()">Créer un compte</a>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Show info alert on page load
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(() => {
                document.getElementById('infoAlert').style.display = 'flex';
            }, 1000);
        });

        // Toggle password visibility
        function togglePassword(inputId) {
            const input = document.getElementById(inputId);
            const button = input.parentNode.querySelector('.password-toggle i');
            
            if (input.type === 'password') {
                input.type = 'text';
                button.classList.remove('fa-eye');
                button.classList.add('fa-eye-slash');
            } else {
                input.type = 'password';
                button.classList.remove('fa-eye-slash');
                button.classList.add('fa-eye');
            }
        }

        // Handle login form submission
        function handleLogin(event) {
            event.preventDefault();
            
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;
            const rememberMe = document.getElementById('rememberMe').checked;
            
            // Simple validation
            if (!username || !password) {
                showAlert('Veuillez remplir tous les champs requis.', 'error');
                return;
            }
            
            // Show loading state
            const submitBtn = event.target.querySelector('.submit-btn');
            const originalText = submitBtn.innerHTML;
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Connexion en cours...';
            submitBtn.disabled = true;
            
            // Simulate API call
            setTimeout(() => {
                // For demo purposes, accept any non-empty credentials
                if (username && password) {
                    showAlert('Connexion réussie ! Redirection vers votre espace...', 'success');
                    
                    // Redirect to official student space after 2 seconds
                    setTimeout(() => {
                        window.location.href = 'https://isetkairouan.edx.tn/login.faces';
                    }, 2000);
                } else {
                    showAlert('Identifiant ou mot de passe incorrect.', 'error');
                    submitBtn.innerHTML = originalText;
                    submitBtn.disabled = false;
                }
            }, 1500);
        }

        // Show alert message
        function showAlert(message, type = 'info') {
            // Remove existing alerts
            const existingAlert = document.querySelector('.alert:not(#infoAlert)');
            if (existingAlert) {
                existingAlert.remove();
            }
            
            // Create new alert
            const alertDiv = document.createElement('div');
            alertDiv.className = `alert alert-${type}`;
            
            let icon = 'info-circle';
            if (type === 'success') icon = 'check-circle';
            if (type === 'error') icon = 'exclamation-circle';
            if (type === 'warning') icon = 'exclamation-triangle';
            
            alertDiv.innerHTML = `
                <i class="fas fa-${icon} alert-icon"></i>
                <div>${message}</div>
            `;
            
            // Insert after info alert or at the beginning of login-right
            const loginRight = document.querySelector('.login-right');
            const infoAlert = document.getElementById('infoAlert');
            
            if (infoAlert && infoAlert.style.display !== 'none') {
                infoAlert.after(alertDiv);
            } else {
                loginRight.insertBefore(alertDiv, loginRight.firstChild);
            }
            
            // Auto-remove alert after 5 seconds (except success which redirects)
            if (type !== 'success') {
                setTimeout(() => {
                    alertDiv.remove();
                }, 5000);
            }
        }

        // Forgot password function
        function showForgotPassword() {
            const email = prompt('Veuillez entrer votre email académique pour réinitialiser votre mot de passe :');
            if (email) {
                if (email.includes('@isetk.rnu.tn')) {
                    showAlert(`Un lien de réinitialisation a été envoyé à ${email}. Veuillez vérifier votre boîte mail.`, 'success');
                } else {
                    showAlert('Veuillez utiliser votre email académique @isetk.rnu.tn', 'warning');
                }
            }
        }

        // Social login function
        function socialLogin(provider) {
            showAlert(`Connexion avec ${provider.charAt(0).toUpperCase() + provider.slice(1)} en cours...`, 'info');
            
            // Simulate social login
            setTimeout(() => {
                showAlert(`Pour utiliser la connexion ${provider}, veuillez utiliser le portail officiel.`, 'warning');
            }, 1000);
        }

        // Register link function
        function showRegister() {
            showAlert('Pour créer un compte étudiant, veuillez contacter le service des inscriptions ou utiliser le portail officiel.', 'info');
            
            // Optionally redirect to official registration page
            setTimeout(() => {
                if (confirm('Souhaitez-vous être redirigé vers le portail officiel des inscriptions ?')) {
                    window.open('https://isetkairouan.edx.tn', '_blank');
                }
            }, 1500);
        }

        // Keyboard shortcuts
        document.addEventListener('keydown', function(e) {
            // Ctrl+Enter to submit form
            if (e.ctrlKey && e.key === 'Enter') {
                document.getElementById('loginForm').dispatchEvent(new Event('submit'));
            }
            
            // Escape to clear form
            if (e.key === 'Escape') {
                document.getElementById('loginForm').reset();
            }
        });

        // Auto-fill demo credentials for testing
        function fillDemoCredentials() {
            document.getElementById('username').value = '20240001';
            document.getElementById('password').value = 'demo123';
            showAlert('Identifiants de démo remplis. Vous pouvez maintenant vous connecter.', 'info');
        }

        // Add demo button for testing (hidden by default)
        const demoButton = document.createElement('button');
        demoButton.innerHTML = '<i class="fas fa-magic"></i> Remplir identifiants de démo';
        demoButton.style.position = 'fixed';
        demoButton.style.bottom = '20px';
        demoButton.style.right = '20px';
        demoButton.style.padding = '10px 15px';
        demoButton.style.background = 'linear-gradient(to right, var(--primary-blue), var(--secondary-blue))';
        demoButton.style.color = 'white';
        demoButton.style.border = 'none';
        demoButton.style.borderRadius = '5px';
        demoButton.style.cursor = 'pointer';
        demoButton.style.fontSize = '12px';
        demoButton.style.zIndex = '1000';
        demoButton.style.boxShadow = '0 3px 10px rgba(0,0,0,0.2)';
        demoButton.onclick = fillDemoCredentials;
        
        // Uncomment next line to enable demo button
        // document.body.appendChild(demoButton);
    </script>
</body>
</html>
